/* Stub for cubing.js search-worker-entry.js — empty IIFE.
   webpack/Terser treats these .js outputs as classic worker scripts and
   rejects ESM export/import syntax. This IIFE has no ESM keywords, so
   Terser can parse it without errors. The project never uses cubing/search.
*/
(function() {})();
